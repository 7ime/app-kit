import {IParentClass} from '@models/shared'
import {CalendarProps} from 'react-calendar'

namespace ICalendar {
    export interface Props extends IParentClass, CalendarProps {

    }
}

export default ICalendar
